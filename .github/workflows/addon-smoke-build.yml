name: addon-smoke-build
on:
  workflow_dispatch:
  repository_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build add-on (Docker parity test)
        run: make -C dev build_amd64

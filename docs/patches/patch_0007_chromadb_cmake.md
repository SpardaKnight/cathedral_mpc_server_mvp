# Patch 0007: Install CMake for chromadb builds

## Summary
- add the `cmake` package to the add-on image so chromadb's hnswlib dependency can compile during build

## Verification
- not run (dependency packaging only)
